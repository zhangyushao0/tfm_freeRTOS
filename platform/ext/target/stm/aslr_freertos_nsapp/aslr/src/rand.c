#include "rand.h"

// extern RNG_HandleTypeDef hrng;
uint32_t rand_generate()
{
    // uint32_t res;
    // uint32_t ret;
    // ret = HAL_RNG_GenerateRandomNumber(&hrng, &res);
    // while(ret==HAL_ERROR)
    // {
    // 	ret = HAL_RNG_GenerateRandomNumber(&hrng, &res);
    // }

    // return res;
    return 0;
}
